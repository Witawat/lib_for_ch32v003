# SimpleTask Library: From Basic to Advanced

## Overview
คู่มือการใช้งานไลบรารี `simple_task` อย่างละเอียด ตั้งแต่พื้นฐานไปจนถึงเทคนิคขั้นสูงสำหรับการทำ Multitasking บนไมโครคอนโทรลเลอร์แกนเดี่ยว (Single-Core)

---

## Level 1: พื้นฐาน (Basic Usage)
เหมาะสำหรับงานทั่วไปที่ไม่ซับซ้อน เช่น การกระพริบไฟ หรืออ่านค่าเซนเซอร์ง่ายๆ

### 1.1 ทำงานวนซ้ำ (Periodic Task)
ใช้ `RUN_EVERY(ms)` เพื่อสั่งให้ทำงานทุกๆ ms
```c
while (1) {
    // งานที่ 1: กระพริบไฟ Heartbeat ทุก 1 วินาที
    RUN_EVERY(1000) {
        printf("Heartbeat: Alive\n");
        ToggleLED();
    }

    // งานที่ 2: อ่านค่า ADC ทุก 100ms
    RUN_EVERY(100) {
        adc_val = ADC_Read();
    }
}
```

### 1.2 ทำงานครั้งเดียว (One-shot Task)
ใช้ `RUN_ONCE(ms)` เพื่อหน่วงเวลาก่อนเริ่มทำงาน (ทำงานแค่รอบเดียวแล้วจบเลย)
```c
while (1) {
    // รอ 3 วินาทีหลังจากเริ่มระบบ แล้วค่อยเริ่มการเชื่อมต่อ
    RUN_ONCE(3000) {
        printf("System Warming up...\n");
        ConnectWiFi();
    }
}
```

---

## Level 2: เทคนิคระดับกลาง (Intermediate)
การจัดการลำดับการทำงานและเงื่อนไข

### 2.1 การกระจายโหลด (Load Balancing)
ถ้ามีงานหนักๆ 10 อย่างที่ต้องทำทุก 1 วินาที อย่าสั่ง `RUN_EVERY(1000)` พร้อมกัน 10 อัน เพราะมันจะทำงาน "ตูมเดียว" พร้อมกันในวินาทีแรก
**เทคนิค:** ใช้การเหลื่อมเวลาเริ่ม (Offset) หรือใช้ Counter
```c
static uint8_t slot = 0;
RUN_EVERY(100) { // ทำงานย่อยๆ ทุก 100ms แทนที่จะตูมเดียว 1000ms
    slot++;
    if (slot == 1) Task_A(); // วินาทีที่ 0.1
    if (slot == 2) Task_B(); // วินาทีที่ 0.2
    if (slot == 10) slot = 0; // วนรอบ
}
```

---

## Level 3: ขั้นสูงสุด (Advanced Multitasking) 🚀
**"True Multitasking Simulation"**
หัวใจสำคัญคือ **"ห้ามใช้ Loop แช่ (Blocking Code)"** ใน Task เด็ดขาด

### 3.1 ปัญหาของการเขียนแบบปกติ (Blocking)
```c
RUN_EVERY(1000) {
    // ❌ ผิด! การเขียนแบบนี้ cpu จะติดอยู่ในนี้ 5 วินาที
    // Task อื่นๆ จะต้องรอจนกว่า loop นี้จะจบ ทำให้ระบบค้าง
    for(int i=0; i<5; i++) {
        ToggleLED();
        Delay_Ms(1000); 
    }
}
```

#### ✅ วิธีแก้ไข: แปลง Loop เป็น State
เราจะใช้ตัวแปร `static` มาช่วยจำว่า "รอบที่เท่าไหร่แล้ว" และ "รอครบเวลายัง"
```c
// ตัวแปรจำสถานะ (static จะจำค่าไว้ได้แม้จะจบฟังก์ชันไปแล้ว)
static int loop_i = 0;       // แทน int i ใน for loop
static uint32_t last_run = 0; // จำเวลาล่าสุด

RUN_EVERY(10) { // รันถี่ๆ ทุก 10ms เพื่อเข้ามาเช็คเวลา (Overhead ต่ำมาก)
    
    // เงื่อนไข: ถ้าเวลาผ่านไป 1000ms ค่อยทำ
    if (TM_Diff(last_run) >= 1000) {
        last_run = TM_Millis(); // รีเซ็ตเวลา
        
        // ทำงานในแต่ละรอบ
        ToggleLED();
        loop_i++;

        // เชื่อนไขจบ Loop (i < 5)
        if (loop_i >= 5) {
            loop_i = 0; // รีเซ็ตพร้อมสำหรับรอบถัดไป
            // หรืออาจจะ Disable Task นี้ไปเลยก็ได้
        }
    }
}
```

### 3.2 ทางแก้: State Machine (Non-Blocking)
เปลี่ยนจากการ "วนลูปจ่อ" เป็นการ "จำสถานะแล้วออกมารับงานอื่น"
```c
// ตัวแปรจำสถานะ (ต้องเป็น static เพื่อให้จำค่าเดิมได้ในรอบถัดไป)
static uint8_t state = 0;
static uint32_t last_time = 0;

RUN_EVERY(1) { // รันถี่ยิบทุก 1ms เพื่อเช็คสถานะ (กิน Resource น้อยมากถ้าไม่เข้าเงื่อนไข)

    // State 0: เริ่มต้นสั่งงาน
    if (state == 0) {
        SendCommand_Step1();
        last_time = millis(); // จำเวลาเริ่ม
        state = 1; // เปลี่ยนไปสถานะรอ
        return; // ออกทันที! เพื่อให้ Task อื่นได้ทำงาน
    }

    // State 1: รอเวลา 5 วินาที (แบบไม่ Blocking)
    if (state == 1) {
        if (TM_Diff(last_time) > 5000) { // ถ้าครบ 5 วิค่อยทำต่อ
             SendCommand_Step2();
             state = 0; // จบงาน หรือวนกลับไปเริ่มใหม่
        }
        // ถ้าเวลายังไม่ครบ มันจะทะลุลงไป return หรือทำ Task อื่นต่อทันที
    }
}
```

### 3.3 ตัวอย่าง: ไฟกระพริบ + รอรับปุ่ม พร้อมกัน (ไม่ค้าง)
ตัวอย่างนี้ชี้ให้เห็นว่าเราสามารถกดปุ่มได้ทันที แม้ไฟกำลังกระพริบอยู่ (ซึ่งทำไม่ได้ถ้าใช้ Delay)

```c
while (1) {
    // งานที่ 1: ไฟกระพริบ (State Machine)
    static uint8_t led_state = 0;
    static uint32_t led_timer = 0;
    
    // ไม่ใช้ RUN_EVERY ครอบใหญ่ แต่ใช้เช็คเวลาเองเพื่อความยืดหยุ่นสูงสุด
    if (TM_Diff(led_timer) >= 500) {
        led_timer = TM_Millis();
        ToggleLED(); // กระพริบทุก 500ms
    }

    // งานที่ 2: เช็คปุ่ม (ทำงานได้ทันที ไม่ต้องรอไฟกระพริบ)
    RUN_EVERY(10) { // เช็คทุก 10ms (Debounce)
        if (ReadButton() == PRESSED) {
            printf("Button Pressed! (Instant)\n");
            // สั่งเปลี่ยนโหมดไฟได้ทันที
        }
    }
}
```

## สรุป
- **Simple Task**: เหมาะกับงานที่จบเร็ว (อ่านค่า, คำนวณ, ส่งค่า)
- **State Machine**: จำเป็นต้องใช้เมื่องานนั้น "ต้องใช้เวลา" (รอเซนเซอร์, รอดีเลย์) แต่อยากให้ทำงานอื่นคู่ขนานได้ด้วย
